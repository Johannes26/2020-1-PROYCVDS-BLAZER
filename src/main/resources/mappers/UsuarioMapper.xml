<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="edu.eci.cvds.persistencia.mybatisimpl.mappers.UsuarioMapper">

    <select parameterType="map" id="consultarUsuario" resultMap="UsuarioResult">
        select
	        u.id as u_id,
	        u.nombre as u_nombre,
	        u.apellido as u_apellido,
	        u.email as u_email,
	        u.telefono as u_telefono,
	        u.tipoUsuario as u_tipoUsuario,
	        u.contrasena as u_contrasena
        from USUARIO u
        WHERE u.email=#{email};
    </select>
    
   <select parameterType="map" id="consultarUsuarios" resultMap="UsuarioResult">
        select
	        u.id as u_id,
	        u.nombre as u_nombre,
	        u.apellido as u_apellido,
	        u.email as u_email,
	        u.telefono as u_telefono,
	        u.tipoUsuario as u_tipoUsuario,
	        u.contrasena as u_contrasena
        from USUARIO u
    </select>
    
    <update id="cambiarRol" parameterType="map">
    	update USUARIO
    	set tipoUsuario= #{rol}
    	where id=#{id};
    </update>

    <resultMap type='Usuario' id='UsuarioResult'>
        <id property='id' column='u_id'/>
        <result property='nombre' column='u_nombre'/>
        <result property='apellido' column='u_apellido'/>
        <result property='email' column='u_email'/>
        <result property='telefono' column='u_telefono'/>
        <result property='tipoUsuario' column='u_tipoUsuario'/>
        <result property='contrasena' column='u_contrasena'/>
    </resultMap>


</mapper>