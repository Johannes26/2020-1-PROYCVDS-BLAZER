<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="edu.eci.cvds.persistencia.mybatisimpl.mappers.IniciativaMapper">

    <select parameterType="map" id="consultarIniciativa" resultMap="IniciativaResult">
        select
	        u.num,
	        u.descripcion,
	        u.usuarioproponente,
	        u.palabrasclave,
	        u.fechaRegistro,
	        u.estado,
	        u.areaProponente
        from INICIATIVA u
        WHERE u.num=#{num};
    </select>

    <select parameterType="map" id="consultarIniciativas" resultMap="IniciativaResult">
        select
            u.num,
            u.descripcion,
            u.usuarioproponente,
        from INICIATIVA u
    </select>
    
    <insert id="insertarIniciativa" parameterType="map">
        INSERT INTO INICIATIVA (num , descripcion , usuarioproponente , palabrasclave, fechaRegistro, estado, areaProponente) VALUES ( #{iniciativa.num}, #{iniciativa.descripcion} , #{iniciativa.usuarioproponente} , #{iniciativa.palabrasclave}, #{iniciativa.fechaRegistro}, #{iniciativa.estado}, #{iniciativa.areaProponente});
    </insert>
    
    <update id="cambiarEstadoIniciativa" parameterType="map">
    	UPDATE INICIATIVA
        SET estado = #{estado}
        WHERE num =  #{INICIATIVA.num};
    </update>

    <resultMap type='Iniciativa' id='IniciativaResult'>
        <id property='num' column='num'/>
        <result property='descripcion' column='descripcion'/>
        <result property='usuarioproponente' column='usuarioproponente'/>
        <result property='palabrasclave' column='palabrasclave'/>
        <result property='fechaRegistro' column='fechaRegistro'/>
        <result property='estado' column='estado'/>
        <result property='areaProponente' column='areaProponente'/>
        
    </resultMap>


</mapper>